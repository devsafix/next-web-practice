import React from 'react';

const productDetailsPage = ({ params }) => {

    console.log(params.details);

    return (
        <div>
            this is details of product
        </div>
    );
};

export default productDetailsPage;